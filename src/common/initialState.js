/* eslint-disable quotes, quote-props, comma-dangle, max-len */
const initialState = {
  "app": {
    "currentLocale": "en",
    "defaultLocale": "en",
    "locales": [ "en", "ru", "ua" ],
  }
};

export default initialState
